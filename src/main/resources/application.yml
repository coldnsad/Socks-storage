spring:
  application:
    name: socks-storage

#  datasource:
#    url: jdbc:postgresql://localhost:5432/socks
#    username: myuser
#    password: mypassword
#    driver-class-name: org.postgresql.Driver

#Docker containered db
  datasource:
    url: jdbc:postgresql://localhost:5433/socks
    username: myuser
    password: mypassword
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true

#logging:
#  level:
#    root: debug
#  file:
#    name: app/app.log

#SWAGGER
#http://localhost:8080/swagger-ui/index.html
springdoc:
  swagger-ui:
      enabled: true
  api-docs:
    enabled: true

api:
  server:
    url: http://localhost:8080
jwt:
  token: jwt12561bit1secret1key1here11234567890d123dasd213121
  expiration: 900000 #15 min